@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h1 class="text-2xl font-bold">Products & Services</h1><p class="text-sm text-gray-500">Items used in quotations, invoices, purchases and stock.</p></div><a href="{{ route('products.create') }}" class="rounded-lg bg-black px-4 py-2 text-sm text-white">New Product</a></div>
<form class="my-4"><input name="q" value="{{ request('q') }}" placeholder="Search product or SKU" class="w-full rounded-lg border px-3 py-2"></form>
<div class="overflow-x-auto rounded-xl border bg-white"><table class="min-w-full text-sm"><thead class="border-b bg-gray-50"><tr><th class="p-3 text-left">SKU</th><th class="p-3 text-left">Name</th><th class="p-3 text-left">Type</th><th class="p-3 text-right">Purchase Cost</th><th class="p-3 text-right">Selling Price</th><th class="p-3 text-right">Stock</th><th class="p-3">Serial</th></tr></thead><tbody>@forelse($products as $product)<tr class="border-b"><td class="p-3">{{ $product->sku ?: '—' }}</td><td class="p-3 font-semibold">{{ $product->name }}</td><td class="p-3">{{ ucfirst($product->item_type) }}</td><td class="p-3 text-right">PKR {{ number_format($product->purchase_cost,2) }}</td><td class="p-3 text-right">PKR {{ number_format($product->default_selling_price,2) }}</td><td class="p-3 text-right">{{ rtrim(rtrim(number_format((float)($stock[$product->id]??0),4), '0'), '.') }} {{ $product->unit?->symbol ?: $product->unit?->name }}</td><td class="p-3">{{ $product->track_serial ? 'Yes' : 'No' }}</td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-gray-500">No products yet.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $products->links() }}</div>
@endsection
