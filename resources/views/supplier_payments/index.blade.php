@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h1 class="text-2xl font-bold">Supplier Payments</h1><p class="text-sm text-gray-500">Payments posted against supplier accounts.</p></div><div class="flex gap-2"><a href="{{ route('payables.index') }}" class="rounded-lg border px-4 py-2 text-sm">Payables</a><a href="{{ route('supplier_payments.create') }}" class="rounded-lg bg-black px-4 py-2 text-sm text-white">New Payment</a></div></div>
<form class="my-4"><input name="q" value="{{ request('q') }}" placeholder="Search payment or supplier" class="w-full rounded-lg border px-3 py-2"></form>
<div class="overflow-x-auto rounded-xl border bg-white"><table class="min-w-full text-sm"><thead class="border-b bg-gray-50"><tr><th class="p-3 text-left">Payment</th><th class="p-3 text-left">Date</th><th class="p-3 text-left">Supplier</th><th class="p-3 text-left">Method</th><th class="p-3 text-right">Amount</th><th></th></tr></thead><tbody>@forelse($payments as $payment)<tr class="border-b last:border-0"><td class="p-3"><a class="font-semibold underline" href="{{ route('supplier_payments.show',$payment) }}">{{ $payment->payment_number }}</a></td><td class="p-3">{{ $payment->payment_date->format('d-M-Y') }}</td><td class="p-3">{{ $payment->supplier->company_name }}</td><td class="p-3">{{ $payment->payment_method }}</td><td class="p-3 text-right">PKR {{ number_format($payment->amount,2) }}</td><td class="p-3 text-right"><a class="underline" href="{{ route('supplier_payments.print',$payment) }}">Print</a></td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-500">No supplier payments found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $payments->links() }}</div>
@endsection
