@extends('layouts.app')
@section('content')
<div class="mb-5 flex items-center justify-between"><div><h1 class="text-2xl font-bold">Stock</h1><p class="text-sm text-gray-500">Current quantity and estimated stock value.</p></div><a href="{{ route('stock_adjustments.create') }}" class="rounded-lg bg-gray-900 px-4 py-2 text-sm font-semibold text-white">Adjust Stock</a></div>
<form class="mb-4 grid gap-2 rounded-xl border bg-white p-4 md:grid-cols-3"><select name="warehouse_id" class="rounded border px-3 py-2">@foreach($warehouses as $w)<option value="{{ $w->id }}" @selected($warehouseId==$w->id)>{{ $w->name }}</option>@endforeach</select><input name="search" value="{{ $search }}" placeholder="Search product / SKU" class="rounded border px-3 py-2"><button class="rounded-lg border px-4 py-2">Search</button></form>
<div class="overflow-x-auto rounded-xl border bg-white"><table class="min-w-full text-sm"><thead class="border-b bg-gray-50"><tr><th class="px-4 py-3 text-left">Product</th><th class="px-4 py-3 text-left">SKU</th><th class="px-4 py-3 text-right">Qty</th><th class="px-4 py-3 text-right">Unit Cost</th><th class="px-4 py-3 text-right">Stock Value</th></tr></thead><tbody class="divide-y">@foreach($rows as $r)<tr><td class="px-4 py-3 font-medium">{{ $r->product->name }}</td><td class="px-4 py-3">{{ $r->product->sku ?: '—' }}</td><td class="px-4 py-3 text-right">{{ number_format($r->qty,3) }}</td><td class="px-4 py-3 text-right">PKR {{ number_format($r->unit_cost,2) }}</td><td class="px-4 py-3 text-right">PKR {{ number_format($r->stock_value,2) }}</td></tr>@endforeach</tbody></table></div>
@endsection
