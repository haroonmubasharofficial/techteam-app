@extends('layouts.app')
@section('content')
<div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold">Customers</h1><p class="text-sm text-gray-500">Customer master and account access.</p></div><a href="{{ route('parties.customer_create') }}" class="rounded-lg bg-black px-4 py-2 text-sm text-white">New Customer</a></div>
<div class="mt-5 overflow-x-auto rounded-xl border bg-white"><table class="min-w-full text-sm"><thead class="border-b bg-gray-50"><tr><th class="p-3 text-left">Company</th><th class="p-3 text-left">Contact</th><th class="p-3 text-left">Phone</th><th class="p-3 text-left">Payment Terms</th><th class="p-3">Status</th><th></th></tr></thead><tbody>@forelse($customers as $customer)<tr class="border-b"><td class="p-3 font-semibold">{{ $customer->company_name }}</td><td class="p-3">{{ $customer->contact_person ?: '—' }}</td><td class="p-3">{{ $customer->phone ?: $customer->mobile ?: '—' }}</td><td class="p-3">{{ $customer->payment_terms ?: '—' }}</td><td class="p-3">{{ $customer->is_active ? 'Active' : 'Inactive' }}</td><td class="p-3 text-right"><a class="underline" href="{{ route('customer_payments.statement',$customer) }}">Statement</a></td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-500">No customers yet.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $customers->links() }}</div>
@endsection
