@extends('layouts.app')
@section('content')
<div class="flex items-center justify-between mb-5"><div><div class="text-sm text-gray-500">Purchasing</div><h1 class="text-2xl font-bold">Purchases</h1></div><a href="{{ route('purchases.create') }}" class="rounded-lg bg-gray-900 px-4 py-2 font-bold text-white">Receive Purchase</a></div>
<div class="overflow-hidden rounded-xl border bg-white"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-gray-50 text-left"><tr><th class="p-3">Purchase #</th><th class="p-3">Date</th><th class="p-3">Supplier</th><th class="p-3">Warehouse</th><th class="p-3 text-right">Total PKR</th><th class="p-3">Status</th></tr></thead><tbody>@forelse($purchases as $p)<tr class="border-t hover:bg-gray-50"><td class="p-3"><a class="font-semibold underline" href="{{ route('purchases.show',$p) }}">{{ $p->purchase_number }}</a></td><td class="p-3">{{ $p->purchase_date->format('d-M-Y') }}</td><td class="p-3">{{ $p->supplier->company_name }}</td><td class="p-3">{{ $p->warehouse->name }}</td><td class="p-3 text-right">{{ number_format($p->total_amount,2) }}</td><td class="p-3">{{ ucfirst($p->status) }}</td></tr>@empty<tr><td colspan="6" class="p-6 text-center text-gray-500">No purchases yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ $purchases->links() }}</div>
@endsection
