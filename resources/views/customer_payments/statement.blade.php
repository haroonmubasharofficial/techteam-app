@extends('layouts.app')
@section('content')
<div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold">Customer Statement</h1><p class="text-sm text-gray-500">{{ $customer->company_name }}</p></div><a href="{{ route('customer_payments.create') }}" class="rounded-lg bg-black px-4 py-2 text-sm text-white">New Receipt</a></div>
<div class="mt-5 overflow-x-auto rounded-xl border bg-white"><table class="min-w-full text-sm"><thead class="border-b bg-gray-50"><tr><th class="p-3 text-left">Date</th><th class="p-3 text-left">Type</th><th class="p-3 text-left">Reference</th><th class="p-3 text-right">Debit</th><th class="p-3 text-right">Credit</th><th class="p-3 text-right">Balance</th></tr></thead><tbody>@forelse($entries as $entry)<tr class="border-b"><td class="p-3">{{ \Illuminate\Support\Carbon::parse($entry->entry_date)->format('d-M-Y') }}</td><td class="p-3">{{ ucfirst($entry->entry_type) }}</td><td class="p-3">{{ $entry->description }}</td><td class="p-3 text-right">{{ $entry->debit ? 'PKR '.number_format($entry->debit,2) : '—' }}</td><td class="p-3 text-right">{{ $entry->credit ? 'PKR '.number_format($entry->credit,2) : '—' }}</td><td class="p-3 text-right font-semibold">PKR {{ number_format($entry->balance,2) }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-500">No ledger entries.</td></tr>@endforelse</tbody></table></div>
<div class="mt-4 rounded-xl border bg-white p-5 text-right"><span class="text-sm text-gray-500">Current Outstanding</span><div class="text-2xl font-bold">PKR {{ number_format(max(0,$balance),2) }}</div></div>
@endsection
